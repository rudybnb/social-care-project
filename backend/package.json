{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src --exec \"ts-node --transpile-only\" src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "migrate": "ts-node --transpile-only migrate.ts",
    "setup": "ts-node --transpile-only setup.ts",
    "generate-qr": "ts-node --transpile-only generate-qr-codes.ts",
    "postbuild": "echo \"Build completed\"",
    "migrate:login": "ts-node --transpile-only migrate-login.ts",
    "migrate:staff-status": "ts-node --transpile-only run-migration.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.6",
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "qrcode": "^1.5.4",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.9.0",
    "@types/pg": "^8.15.5",
    "@types/qrcode": "^1.5.5",
    "drizzle-kit": "^0.31.5",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}